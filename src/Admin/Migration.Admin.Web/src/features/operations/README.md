# Operations

Canonical home for runtime dashboard, run detail, execution sessions, failure retry, worker telemetry, and operational events surfaces as they are consolidated from top-level `pages`, `api`, and `types` folders.
