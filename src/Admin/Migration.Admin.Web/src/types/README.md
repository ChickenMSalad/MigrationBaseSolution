# Admin Web Shared Types

This folder contains shared Admin Web type surfaces that are not owned by a single feature.

Feature-specific types should live with their feature under `src/features/<domain>/<feature>/types`.

Do not add new feature-only types here unless they are intentionally shared by multiple feature areas.
