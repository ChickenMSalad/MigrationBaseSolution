# P4.47 — Operational Event Timeline UI

This set adds an operator UI workspace for the durable event query endpoint from P4.46.

The workspace calls:

```text
GET /api/operational/events/query
```

Supported filters:

- severity
- category
- event type
- skip/take paging

The workspace is added to the existing `OperationalWorkspaceComposition`.
