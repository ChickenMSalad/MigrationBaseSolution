# P4.85 — Replay Admission Manual Controls UI

This set exposes P4.84 manual replay admission controls in the execution-session workspace.

The selected session view gets:

- manual admission operator field
- manual admission reason field
- force admit button
- force defer button

The backend requires operator and reason for both actions.
