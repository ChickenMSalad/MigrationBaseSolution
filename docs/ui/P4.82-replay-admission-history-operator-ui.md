# P4.82 — Replay Admission History Operator UI

This set exposes durable replay admission history in the execution-session workspace.

The selected session view now shows:

- admission decision timestamp
- admitted/deferred decision
- deferred/admitted reason
- active replay count at decision time
- max concurrent replay limit at decision time
- allowed-window status at decision time

Backend behavior remains from P4.81.
