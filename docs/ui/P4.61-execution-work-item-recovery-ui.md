# P4.61 — Execution Work Item Recovery UI

This set adds UI controls for P4.60 work-item recovery.

Capabilities:

- renew a leased work item
- requeue failed work items
- requeue expired leases
- tune lease seconds from UI

This lets operators recover stalled local/dev execution work without direct API calls.
