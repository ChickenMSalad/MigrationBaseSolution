# P4.77 — Replay Admission Operator UI

This set exposes P4.76 replay admission governance in the UI.

The execution-session workspace gets:

- admission take input
- evaluate admission button
- active replay count
- configured max concurrency
- allowed-window status
- admitted/deferred decisions table

Backend behavior remains in P4.76.
