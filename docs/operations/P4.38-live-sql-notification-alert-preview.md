# P4.38 — Live SQL Notification Alert Preview

This set moves the notification summary and alert preview endpoints onto `ISqlOperationalMetricsReader`.

The endpoint now surfaces alerts for:

- SQL metric reader unhealthy/not configured
- existing migration failures
- pending operational queue work

Route family:

```text
/api/operational/notifications
```

This remains intentionally thin and read-only.
