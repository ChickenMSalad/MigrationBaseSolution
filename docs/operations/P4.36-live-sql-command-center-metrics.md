# P4.36 — Live SQL Command Center Metrics

This set replaces placeholder command-center metrics with live SQL-backed counts.

The command-center summary now reads:

- `MigrationRuns`
- `MigrationWorkItems`
- `MigrationFailures`

from the operational SQL database.

Route:

```text
/api/operational/command-center/summary
```

The endpoint intentionally remains lightweight and read-only.
