# P4.37 — SQL Operational Metrics Reader

This set introduces a small Admin API-local abstraction for SQL operational metrics:

```csharp
ISqlOperationalMetricsReader
```

The command-center endpoint now consumes the reader instead of embedding SQL counting logic directly.

This keeps the endpoint thin and prepares later endpoints to reuse the same operational snapshot without duplicating raw SQL.
