# P4.34 — Operational SQL Health Check Surface

This set adds a thin SQL health check route:

```text
/api/operational/sql/health
```

The endpoint verifies:

- Operational SQL connection string is configured
- SQL Server is reachable
- core operational tables exist

This is intended for local/dev runtime confidence after P4.32 and P4.33.
