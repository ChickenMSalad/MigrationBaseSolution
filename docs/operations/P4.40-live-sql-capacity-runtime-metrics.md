# P4.40 — Live SQL Capacity Runtime Metrics

This set moves operational capacity/runtime endpoints onto `ISqlOperationalMetricsReader`.

Route family:

```text
/api/operational/capacity
```

Current SQL-backed runtime indicators:

- queue depth
- runtime pressure
- failure count
- operational SQL health state

The endpoint family remains intentionally read-only and compositional.
