# Admin Web Operational Events Client

This page/client brings operational event timeline visibility into the canonical Admin Web.

The `apps/migration-admin-ui` project remains the feature-source only. The canonical deployable UI is `src/Admin/Migration.Admin.Web`.

The page intentionally reads operational events from the Admin API and does not mutate runtime state.
