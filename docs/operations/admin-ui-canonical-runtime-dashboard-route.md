# Admin UI Canonical Runtime Dashboard Route

The canonical Admin UI is `src/Admin/Migration.Admin.Web`.

The feature-source UI at `apps/migration-admin-ui` must not receive new feature work until its useful pieces are migrated into the canonical app. Runtime dashboard visibility belongs in the canonical app.

P10.2G wires the Runtime Dashboard route and navigation link into the canonical Admin Web app.
