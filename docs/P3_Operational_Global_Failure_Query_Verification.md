# P3 Operational Global Failure Query Verification

Verifies that the global failure query endpoint is mapped and consistent with the recent failures feed.

## Run

```powershell
./scripts/operational-global-failure-query-full-smoke-test.ps1 -BaseUrl "https://localhost:55436"
```
