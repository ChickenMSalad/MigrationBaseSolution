# P3 Operational Global Failure Filtered Analytics Verification

Verifies that filtered failure analytics remain internally consistent for the same filter set.

## Run

```powershell
./scripts/operational-global-failure-filtered-analytics-full-smoke-test.ps1 -BaseUrl "https://localhost:55436"
```
