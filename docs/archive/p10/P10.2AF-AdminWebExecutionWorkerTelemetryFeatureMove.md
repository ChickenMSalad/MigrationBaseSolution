# P10.2AF - Admin Web Execution Worker Telemetry Feature Move

## Purpose

Continue the Admin Web canonical feature folder migration without broad route or architecture changes.

## Canonical target

`src/Admin/Migration.Admin.Web/src/features/operations/executionWorkerTelemetry/`

## Notes

This follows the P10.2AB through P10.2AE pattern and keeps the cloud deployable Admin UI at `src/Admin/Migration.Admin.Web`.

The apply script validates every required source file before moving anything, so it will not partially move the feature if the current repo state is not what the set expects.
