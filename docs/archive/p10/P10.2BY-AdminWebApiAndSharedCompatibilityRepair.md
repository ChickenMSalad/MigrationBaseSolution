# P10.2BY - Admin Web API and Shared Compatibility Repair

This set addresses the compile bucket exposed after P10.2BU/BV/BW/BX:

- API helper compatibility and missing `adminApiClient`/`apiPost` symbols.
- Shared component prop compatibility for migrated pages.
- Missing Preflight API bridge.

Runtime dashboard model alignment and credential vault model alignment are intentionally left for later targeted sets.
