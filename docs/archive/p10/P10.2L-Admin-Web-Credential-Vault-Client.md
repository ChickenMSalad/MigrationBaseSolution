# P10.2L Admin Web Credential Vault Client

## Purpose

Migrate the credential-vault operator view from the feature-source admin UI into the canonical Admin Web application.

Canonical Admin UI:

```text
src/Admin/Migration.Admin.Web
```

Feature source only:

```text
apps/migration-admin-ui
```

This set adds canonical Admin Web client files only. It does not make `apps/migration-admin-ui` deployable and does not remove feature-source files.

## Added canonical files

```text
src/Admin/Migration.Admin.Web/src/types/credentialVault.ts
src/Admin/Migration.Admin.Web/src/api/credentialVaultApi.ts
src/Admin/Migration.Admin.Web/src/pages/CredentialVault.tsx
```

## API endpoints used

```text
GET  /api/operational/connectors/credentials/summary
GET  /api/operational/connectors/credentials/catalog
POST /api/operational/connectors/credentials/validate
```

## Follow-up

Route and navigation wiring should be handled in a separate bounded set after this page builds cleanly in the canonical Admin Web application.
