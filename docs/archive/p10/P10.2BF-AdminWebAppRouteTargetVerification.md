# P10.2BF - Admin Web App Route Target Verification

Purpose: verify canonical Admin Web `App.tsx` against the local working tree after the consolidation batches.

This set is report-only. It checks:

- duplicate import lines
- malformed import lines
- imported source files that do not resolve locally
- route component symbols that are not imported
- imports that are not referenced by routes or JSX body
- duplicate route paths

Generated report:

`docs/P10/P10.2BF-AdminWebAppRouteTargetVerification.Report.md`
