# P10.2AL - Admin Web Capacity Forecast Feature Move

Moves the canonical Admin Web Capacity Forecast feature from the flat `pages/`, `api/`, and `types/` layout into the feature folder structure:

`src/Admin/Migration.Admin.Web/src/features/platform/capacityForecast`

This set is intentionally narrow and does not add route wiring. Current `App.tsx` on main does not reference `CapacityForecast`, so the set only consolidates the existing page/API/type files and updates the API core-client import.

## Apply

```powershell
powershell -ExecutionPolicy Bypass -File .\tools\p10\P10.2AL\Apply-P10.2AL-AdminWebCapacityForecastFeatureMove.ps1
powershell -ExecutionPolicy Bypass -File .\tools\p10\P10.2AL\Test-P10.2AL-AdminWebCapacityForecastFeatureMove.ps1
```
