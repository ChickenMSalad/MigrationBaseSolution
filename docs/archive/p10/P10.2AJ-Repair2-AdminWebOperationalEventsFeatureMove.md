# P10.2AJ Repair2 - Admin Web Operational Events Feature Move

## Intent

Fix the AJ repair validator self-reference failure and complete validation of the Operational Events feature move.

## Scope

Canonical Admin Web only:

- `src/Admin/Migration.Admin.Web/src/features/operations/operationalEvents`
- `src/Admin/Migration.Admin.Web/src/App.tsx`

## Safety

- Handles files already moved by the original AJ package.
- Validates source-or-destination existence before attempting moves.
- Uses `[pscustomobject]` records, not nested validation arrays.
- Avoids `$variable:` interpolation.
- Uses `System.IO.Path.Combine` through `Join-RepoPath`.
- The validator scans the apply script only and constructs risky pattern strings without literal corrupted escapes.
