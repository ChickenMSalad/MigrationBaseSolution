# P10.2AN Repair - Credential Vault Feature Move

This repair completes the P10.2AN Credential Vault consolidation after the apply script failed on the API core client import.

The intended feature location remains:

```text
src/Admin/Migration.Admin.Web/src/features/security/credentialVault/
  pages/CredentialVault.tsx
  api/credentialVaultApi.ts
  types/credentialVault.ts
```

The repair tolerates these states:

- Files already moved into the feature folder.
- Files not yet moved from flat `pages`, `api`, and `types` folders.
- Page imports already corrected.
- API import still using `./core/adminApiClient`.
- `App.tsx` still importing from `./pages/CredentialVault`.

The validator uses anchored import-line regular expressions so corrected paths such as `../../../../components/Card` are not falsely rejected because they contain a shorter legacy substring.
