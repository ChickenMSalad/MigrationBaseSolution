# P10.2AS - Admin Web Residual API/Types Feature Batch Move

Purpose: continue Admin Web consolidation faster by moving remaining flat canonical Admin Web pages/api/types/css for a managed feature map into `src/features/...` folders.

This set is state-based and idempotent. It only moves source files that actually exist locally, does not overwrite existing feature files, and updates `App.tsx` imports only when old flat page imports are present.

Apply:

```powershell
powershell -ExecutionPolicy Bypass -File .\tools\p10\P10.2AS\Apply-P10.2AS-AdminWebResidualApiTypesFeatureBatchMove.ps1
powershell -ExecutionPolicy Bypass -File .\tools\p10\P10.2AS\Test-P10.2AS-AdminWebResidualApiTypesFeatureBatchMove.ps1
```
