# P10.2BI - Admin Web Apps Reference Quarantine

Purpose: preserve residual `/apps/migration-admin-ui` feature-source folders that were copied into canonical Admin Web, but move the ungrouped apps-style feature folders out of the compiled `src/features` tree.

This keeps the canonical deployable Admin Web focused on grouped feature folders such as `operations`, `governance`, `platform`, `security`, and `connectors`, while retaining apps reference material under:

`src/Admin/Migration.Admin.Web/reference/apps-migration-admin-ui/src/features`

This set does not rewrite routes, does not touch `App.tsx`, and does not delete files.

## Apply

```powershell
powershell -ExecutionPolicy Bypass -File .\tools\p10\P10.2BI\Apply-P10.2BI-AdminWebAppsReferenceQuarantine.ps1
powershell -ExecutionPolicy Bypass -File .\tools\p10\P10.2BI\Test-P10.2BI-AdminWebAppsReferenceQuarantine.ps1
```
