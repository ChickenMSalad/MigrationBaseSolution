# P10.2AO Repair3 - Admin Web Connector Configuration Feature Move

This repair is intentionally narrow. It does not introduce a new feature and does not touch `/apps/migration-admin-ui`.

It validates and normalizes the canonical Admin Web Connector Configuration feature under:

`src/Admin/Migration.Admin.Web/src/features/connectors/configuration`

The repair avoids brittle exact legacy-line assumptions and normalizes import source paths by anchored import-line parsing.
