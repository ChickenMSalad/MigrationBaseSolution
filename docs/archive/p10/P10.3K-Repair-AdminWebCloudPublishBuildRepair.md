# P10.3K Repair - Admin Web Cloud Publish Build Repair

Purpose: repair builder page Card prop usage so the production cloud publish package can build.

- Updated `features/governance/mappingBuilder/pages/MappingBuilder.tsx`: replaced `message={message}` with `description={message}`.
- Updated `features/governance/taxonomyBuilder/pages/TaxonomyBuilder.tsx`: replaced `message={message}` with `description={message}`.

No cloud resource names, credentials, or deployment targets were changed.
