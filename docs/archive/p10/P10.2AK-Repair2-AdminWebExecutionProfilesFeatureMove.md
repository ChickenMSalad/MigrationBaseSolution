# P10.2AK Repair2 - Admin Web Execution Profiles Feature Move

## Scope

- Canonical UI only: `src/Admin/Migration.Admin.Web`.
- Handles partial state from P10.2AK / P10.2AK-Repair.
- Does not add an unused `ExecutionProfiles` import to `App.tsx` when the current app has no route usage for it.

## Expected end state

- `ExecutionProfiles.tsx` exists under `src/features/operations/executionProfiles/pages`.
- `executionProfilesApi.ts` exists under `src/features/operations/executionProfiles/api`.
- `executionProfiles.ts` exists under `src/features/operations/executionProfiles/types`.
- Flat legacy files for this feature are absent.
- Imports inside the moved feature files are repo-relative and build-safe.
