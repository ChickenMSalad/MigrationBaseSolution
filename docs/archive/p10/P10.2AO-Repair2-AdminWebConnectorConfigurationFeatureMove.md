# P10.2AO Repair2 — Admin Web Connector Configuration Feature Move

P10.2AO moved Connector Configuration into the canonical Admin Web feature tree, but the first repair validator used a brittle assertion that expected a specific imported symbol named `connectorConfigurationApi`.

The actual page may import named API functions from `connectorConfigurationApi.ts`, so this repair validates the import source path instead of a guessed symbol.

Canonical feature path:

```text
src/Admin/Migration.Admin.Web/src/features/connectors/configuration/
  pages/ConnectorConfiguration.tsx
  api/connectorConfigurationApi.ts
  types/connectorConfiguration.ts
```

This set does not introduce new UI behavior and does not touch `/apps/migration-admin-ui`.
