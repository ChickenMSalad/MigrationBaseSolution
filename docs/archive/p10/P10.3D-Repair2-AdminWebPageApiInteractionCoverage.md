# P10.3D Repair2 - Admin Web Page API Interaction Coverage

Repairs the P10.3D Repair coverage runner/test parse issue caused by fragile regex quoting.

Behavior:
- Uses simple string scanning for `/api/` candidates instead of regex quote parsing.
- Classifies 405 responses for action/probe-style endpoints as verb-mismatch evidence, not acceptance failures.
- Keeps bounded request timeouts and writes summary/details artifacts.
- Does not change Admin Web or Admin API source files.
