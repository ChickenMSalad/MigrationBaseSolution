# P10.2BX - Admin Web Shared Compatibility Repair

## Scope

Targets the TypeScript build errors surfaced after dependency restore and syntax repair:

- `adminApiClient` missing at feature API call sites
- one-generic `apiPost<TResponse>` compatibility
- `apiDelete<TResponse>` compatibility
- `Card` accepting `description`
- `LoadingError` accepting `title`
- Operational Events page component import paths

## Non-goals

- Runtime dashboard model alignment
- Credential vault type alignment
- route reshaping
- file moves
