# P10.2AH - Admin Web Audit Trail Feature Move

This set continues P10.2 Admin UI consolidation by moving one canonical governance feature family from flat folders into `src/features`.

Target feature folder:

`src/Admin/Migration.Admin.Web/src/features/governance/auditTrail`

The set intentionally does not retire `/apps/migration-admin-ui` and does not introduce new routes or architecture.
