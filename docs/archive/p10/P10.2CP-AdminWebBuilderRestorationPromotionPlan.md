# P10.2CP - Admin Web Builder Restoration Promotion Plan

Purpose:

- Confirm whether Manifest Builder, Taxonomy Builder, and Mapping Builder assets are canonical, reference-only, legacy-only, or missing.
- Check local `App.tsx` and `Layout.tsx` for route/navigation references.
- Produce an actionable promotion plan without changing source.

This set intentionally avoids guessing component names or routes.
