# P10.2AK Repair — Admin Web Execution Profiles Feature Move

This set repairs P10.2AK without advancing scope.

It is intentionally idempotent for either state:

- source files still flat under `src/Admin/Migration.Admin.Web/src/pages`, `api`, and `types`
- or files already moved under `src/Admin/Migration.Admin.Web/src/features/operations/executionProfiles`

The script updates the canonical Admin Web only.
