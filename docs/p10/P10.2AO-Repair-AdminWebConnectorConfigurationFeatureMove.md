# P10.2AO Repair — Admin Web Connector Configuration Feature Move

This repair completes validation for the Connector Configuration feature move under:

`src/Admin/Migration.Admin.Web/src/features/connectors/configuration`

It does not introduce a new feature move. It repairs the failed P10.2AO validator assumption that the API module must import `adminApiClient`.
