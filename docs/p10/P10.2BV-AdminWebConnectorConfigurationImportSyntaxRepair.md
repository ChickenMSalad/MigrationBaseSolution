# P10.2BV - Admin Web Connector Configuration Import Syntax Repair

Targets only:

`src/Admin/Migration.Admin.Web/src/features/connectors/configuration/pages/ConnectorConfiguration.tsx`

The build reached TypeScript and reported unterminated import strings in this page. This set normalizes only known consolidation-related import sources in that page:

- `../api/connectorConfigurationApi`
- `../types/connectorConfiguration`
- `../../../../components/Card`
- `../../../../components/LoadingError`

It removes `.tsx` import extensions when present and does not move files or edit routes.
