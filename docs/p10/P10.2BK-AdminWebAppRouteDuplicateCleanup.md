# P10.2BK - Admin Web App Route Duplicate Cleanup

This package performs targeted `App.tsx` cleanup only:

- Remove duplicate `ConnectorConfiguration` route entries after the first retained route.
- Remove unused `OperationalEvents` import when there is no `<OperationalEvents` usage outside imports.
- Preserve the rest of `App.tsx` formatting and route body.

PowerShell safety notes:

- PowerShell 5.1 compatible.
- `Set-StrictMode -Version 2.0` safe.
- No report helper functions with collection parameters.
- No nested validation arrays.
- No self-scanning validator pattern checks.
- No broad source mutation.
