# P10.2R - Admin Web Audit Trail Client

## Purpose

Continue Admin UI consolidation by moving the audit-trail operator surface from the feature-source app into the canonical Admin Web application.

- Canonical deployable UI: `src/Admin/Migration.Admin.Web`
- Feature-source/reference UI: `apps/migration-admin-ui`

This set is intentionally additive. It does not route the page yet and does not modify `/apps/migration-admin-ui`.

## Adds

- `src/Admin/Migration.Admin.Web/src/types/auditTrail.ts`
- `src/Admin/Migration.Admin.Web/src/api/auditTrailApi.ts`
- `src/Admin/Migration.Admin.Web/src/pages/AuditTrail.tsx`

## API endpoints used

- `/api/operational/audit-trail/summary`
- `/api/operational/audit-trail/recent?take=50`

## Follow-up

Route/navigation wiring should be handled in a separate bounded set after the committed `App.tsx` and `Layout.tsx` shapes are verified from GitHub.
