# P10.2BZ - Admin Web Final Compile Bucket Repair

This set is intentionally narrow. It rewrites only the files identified by the current Admin Web TypeScript build output:

- `src/Admin/Migration.Admin.Web/src/api/artifactManifestIndex.ts`
- `src/Admin/Migration.Admin.Web/src/api/artifactStorage.ts`
- `src/Admin/Migration.Admin.Web/src/api/artifactStorageBridge.ts`
- `src/Admin/Migration.Admin.Web/src/api/cloudBinaryStorage.ts`
- `src/Admin/Migration.Admin.Web/src/features/operations/runtimeDashboard/pages/RuntimeRunDetail.tsx`

The API files are aligned to the canonical two-generic `apiPost<TRequest,TResponse>` compatibility shape introduced in the current uncommitted chain. `RuntimeRunDetail.tsx` is aligned to `RuntimeDashboardRunDetail` where run metadata is nested under `detail.run`.
