# P10.2AK Repair3 — Admin Web Execution Profiles Feature Move

P10.2AK moved Execution Profiles files into the canonical feature folder, but its validation logic expected a stale API client import path.

This repair confirms the correct canonical feature shape:

```text
src/Admin/Migration.Admin.Web/src/features/operations/executionProfiles/
  pages/ExecutionProfiles.tsx
  api/executionProfilesApi.ts
  types/executionProfiles.ts
```

`App.tsx` currently does not reference `ExecutionProfiles`, so this set does not add an unused import or route.
