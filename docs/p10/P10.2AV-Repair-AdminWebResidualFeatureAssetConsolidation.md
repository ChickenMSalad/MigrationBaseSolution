# P10.2AV Repair - Admin Web Residual Feature Asset Consolidation

Fixes the AV apply script parser failure by replacing the quoted regex import parser with line-based import scanning that is safe under Windows PowerShell 5.1 and `Set-StrictMode -Version 2.0`.

The repair keeps the AV intent: consolidate residual flat feature API/type assets into existing canonical feature folders and normalize feature-local imports.
