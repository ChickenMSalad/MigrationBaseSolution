# P10.2AT-Repair — Admin Web Canonical Import Sweep Repair

P10.2AT failed because the apply script used a StrictMode-unsafe match-collection length check against a value that could become scalar/null.

This repair replaces that approach with array-normalized match handling and local-state import resolution.

## Guardrails

- PowerShell 5.1 compatible.
- `Set-StrictMode -Version 2.0` safe.
- No nested structured validation arrays.
- No unsafe variable-plus-colon interpolation.
- No recursive validator self-scanning.
- No broad route rewrites.
- No file moves.

## Output

The apply script writes:

`docs/P10/P10.2AT-Repair-AdminWebCanonicalImportSweep.md`
