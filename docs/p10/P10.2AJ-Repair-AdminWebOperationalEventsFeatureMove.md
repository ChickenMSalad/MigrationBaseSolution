# P10.2AJ Repair - Admin Web Operational Events Feature Move

## Intent

Repair the P10.2AJ partial-apply state by completing the canonical `App.tsx` import update for Operational Events while preserving the already moved feature files.

## Scope

Canonical Admin Web only. No `/apps/migration-admin-ui` changes.

The repair handles either state:

- original flat files still present; or
- page/API/types already moved to `src/features/operations/operationalEvents`.

## Safety

- Validates every source-or-destination file before moving.
- Does not move anything until all required file paths are accounted for.
- Uses `[pscustomobject]` validation records, not nested arrays.
- Avoids `$variable:` interpolation.
- Uses `System.IO.Path.Combine` through `Join-RepoPath`.
- Test script scans only this package's scripts.
