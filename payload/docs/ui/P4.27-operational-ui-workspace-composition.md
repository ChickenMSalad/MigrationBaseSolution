# P4.27 — Operational UI Workspace Composition

This set consolidates the operator UI workspace registrations.

Before, `App.tsx` directly imported and rendered each workspace added by P4.11 through P4.25.

After, `App.tsx` imports and renders:

```tsx
<OperationalWorkspaceComposition />
```

The composition component owns the ordering of operational workspaces.
